#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 *mul - entry point
 *@a : number to multiply
 *@b : number to multiply
 *Description: 'multiply numbers'
 * Return: the result (Success)
 */
int mul(int a, int b)
{
	int m;

	m = a * b;
	return (m);
}
