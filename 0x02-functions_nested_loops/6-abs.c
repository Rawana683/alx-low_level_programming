#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - Entry point
 * @c : the number to check
 * Description: 'function to print the absolute value'
 * Return: the absolute value of int. (Success)
 */
int _abs(int c)
{
	abs(c);
	return (c);
}
