#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcpy - entry point
 * @dest : char to use
 * @src :  char to use
 * Desription: 'pointer to pointer
 * Return: the value of dest
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
}
