#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen_recursion - entry point
 * @s : char to use
 *Description : "lenght of string"
 * Return: i
 */
int _strlen_recursion(char *s)
{
	int i;

	i = strlen(s);
	return (i);
}
