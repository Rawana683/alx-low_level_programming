#include <stdio.h>
/**
*main - Entry point
*Description: 'this program is to print compined numbers'
*Return : Always (0) (Success)
*/
int main(void)
{
	int n[10];
	int i[10];
	for (n[0] = 48, i[0] = 57 ; n < 58 && i > 48 ; n++, i--)
{
	putchar(n[], i[]);
	putchar(i[], n[]);
if (n != 57)
{
	putchar(',');
	putchar(' ');
}
}
putchar('\n');
return (0);
}

