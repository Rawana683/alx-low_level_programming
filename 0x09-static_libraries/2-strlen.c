#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - entry point
 * @s : the char to use0
 * Description: 'print the lenght of string'
 * Return: the lenght (success)
 */
int _strlen(char *s)
{
	int l;

	l = strlen(s);
	return (l);
}
